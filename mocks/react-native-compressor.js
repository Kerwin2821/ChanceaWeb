const Compressor = {
    Video: {
        compress: async (uri) => uri,
    },
    Image: {
        compress: async (uri) => uri,
    },
    Audio: {
        compress: async (uri) => uri,
    },
};

export default Compressor;
