const firebase = {
    initializeApp: () => ({
        messaging: () => { },
        firestore: () => { },
    }),
    app: () => ({
        messaging: () => { },
        firestore: () => { },
    }),
    apps: [],
};

export default firebase;
